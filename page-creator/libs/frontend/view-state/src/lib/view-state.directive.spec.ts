import { ViewStateDirective } from './view-state.directive';

describe('ViewStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewStateDirective();
    expect(directive).toBeTruthy();
  });
});
